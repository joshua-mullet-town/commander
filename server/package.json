{
  "name": "commander-server",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "WebSocket game server for Commander's Flag War - the ChatGPT-controlled RTS game.",
  "scripts": {
    "dev": "NODE_OPTIONS='--max-old-space-size=8192' tsx watch src/movement-server.ts",
    "build": "echo '🔨 Building with tsup...' && tsup src/movement-server.ts --format esm --target es2022 --out-dir dist && echo '✅ Build complete - dist/ created'",
    "start": "echo '🚀 Starting server from dist/movement-server.js' && node dist/movement-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "@types/ws": "^8.18.1",
    "minimaxer": "^3.4.0",
    "openai": "^6.9.0",
    "ws": "^8.18.3",
    "zod": "^3.23.8",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "tsup": "^8.0.0"
  },
  "devDependencies": {
  }
}
